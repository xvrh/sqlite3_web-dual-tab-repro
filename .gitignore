# Pub workspace
.dart_tool/
.packages
pubspec.lock

# IDE / agent scratch
.claude/
.idea/
.vscode/

# Build artifacts that aren't useful to commit (.deps and source maps
# are large and regenerated by compile.sh). Keep the .js outputs and
# sqlite3.wasm so the repro runs without a Dart toolchain.
build/*.deps
build/*.map

.*
